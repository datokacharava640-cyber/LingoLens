# 1. სისტემური დამოკიდებულებების დაინსტალირება
!sudo apt-get update
!sudo apt-get install -y build-essential libsqlite3-dev sqlite3 bzip2 libbz2-dev zlib1g-dev libssl-dev openssl libgdbm-dev libgdbm-compat-dev liblzma-dev libreadline-dev libncursesw5-dev libffi-dev uuid-dev git ccache ffmpeg libsdl2-dev libsdl2-image-dev libsdl2-mixin-dev libsdl2-ttf-dev libportmidi-dev libswscale-dev libavformat-dev libavcodec-dev zlib1g-dev openjdk-17-jdk zip unzip

# 2. Cython-ისა და Buildozer-ის დაინსტალირება
!pip install --upgrade pip setuptools
!pip install cython==0.29.36 buildozer

# 3. Buildozer-ის გაშვება Android APK-ს შესაქმნელად
!buildozer -v android debug
